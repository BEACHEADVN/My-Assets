name: bat link tu Tracker

on:
  repository_dispatch:
    types: [trigger_from_tracker]

jobs:
  forward-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Original Build Workflow
        run: |
          # Lấy link ROM từ payload do tracker bắn sang
          ROM_URL="${{ github.event.client_payload.rom_url }}"
          
          echo "🔗 Nhận link tự động từ Tracker: $ROM_URL"
          echo "🚀 Đang kích hoạt workflow build gốc..."
          
          # Tự gọi chính workflow build.yml gốc bằng API workflow_dispatch
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/build.yml/dispatches \
            -d "{\"ref\":\"${{ github.ref_name }}\",\"inputs\":{\"input_url\":\"$ROM_URL\",\"builder_name\":\"AutoTracker\",\"builder_id\":\"system\"}}"
